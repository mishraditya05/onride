package com.onride.auth_service.exception;

public enum ErrorCode {
    VALIDATION_FAILED,
    MALFORMED_REQUEST,
    EMAIL_ALREADY_EXISTS,
    INVALID_CREDENTIALS,
    RESOURCE_NOT_FOUND,
    INTERNAL_ERROR
}
