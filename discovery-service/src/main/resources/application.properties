spring.application.name=discovery-service

server.port=8761

# This IS the registry — it shouldn't register with or fetch from itself
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false